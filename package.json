{
  "name": "fillitup",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm dlx web-ext build --source-dir . --artifacts-dir web-ext-artifacts",
    "sign": "pnpm dlx web-ext sign --api-key $WEB_EXT_API_KEY --api-secret $WEB_EXT_API_SECRET --channel unlisted --source-dir . --artifacts-dir web-ext-artifacts",
    "build-and-sign": "./scripts/build_and_sign.sh"
  },
  "devDependencies": {}
}
